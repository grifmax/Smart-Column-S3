# Установка Node.js на Timeweb

## ❌ Текущая ситуация

Node.js не установлен на вашем хостинге. Это нормально для shared хостинга.

---

## ✅ Решение: Обращение в поддержку Timeweb

### Вариант 1: Через тикет в панели (Рекомендуется)

1. В панели Timeweb найдите раздел **"Поддержка"** или **"Тикеты"**
2. Создайте новый тикет
3. Тема: **"Включить Node.js для аккаунта"**
4. Текст сообщения:

```
Здравствуйте!

Прошу включить Node.js для моего аккаунта co111685.
Нужна версия Node.js 18.x или 20.x для запуска приложения.

Спасибо!
```

5. Отправьте тикет

**Обычно это делается в течение 1-24 часов.**

### Вариант 2: Через онлайн-чат

1. Найдите кнопку **"Онлайн-чат"** или **"Поддержка"** в панели
2. Напишите оператору:
   - "Здравствуйте, нужно включить Node.js для аккаунта co111685"

---

## 🔍 Проверка альтернативных методов

Пока ждете ответа поддержки, можно проверить:

### Проверка через nvm (Node Version Manager)

В SSH-консоли выполните:

```bash
# Проверка nvm
nvm --version

# Если nvm установлен:
nvm install 18
nvm use 18
node --version
```

### Проверка через другие пути

```bash
# Проверка альтернативных путей
which node
whereis node

# Проверка через полный путь (если установлен в нестандартном месте)
/usr/bin/node --version
/usr/local/bin/node --version
~/.local/bin/node --version
```

**Сообщите мне:** Что показывают эти команды?

---

## 📋 Что делать пока ждете

Пока ждете ответа поддержки, можно подготовить файлы:

### ШАГ 1: Создание директории

В SSH-консоли:

```bash
cd ~
mkdir -p smart-column-proxy
cd smart-column-proxy
pwd
```

### ШАГ 2: Загрузка файлов через Git

```bash
# Проверка Git
git --version

# Если Git установлен:
git clone https://github.com/grifmax/Smart-Column-S3.git temp
mv temp/cloud_proxy/* .
mv temp/cloud_proxy/.* . 2>/dev/null || true
rm -rf temp

# Проверка файлов
ls -la
```

**Должны увидеть:** `server.js`, `package.json`

### ШАГ 3: Загрузка через файловый менеджер

Если Git не работает:

1. В панели Timeweb: **"Файл. менеджер"**
2. Перейдите в папку `smart-column-proxy`
3. Загрузите файлы из локальной папки `cloud_proxy`:
   - `server.js`
   - `package.json`

---

## ⚠️ Альтернативные решения

### Если Node.js недоступен

#### Вариант A: Использование PHP-прокси

Можно переписать прокси на PHP (если PHP доступен).

**Проверка PHP:**
```bash
php --version
```

Если PHP доступен - могу создать PHP версию прокси.

#### Вариант B: Использование другого хостинга

- Railway.app (бесплатно, 5 минут)
- VPS с Node.js
- Другие облачные платформы

---

## 📝 Чек-лист

- [ ] Создан тикет в поддержку Timeweb
- [ ] Проверен nvm (если доступен)
- [ ] Создана директория `smart-column-proxy`
- [ ] Загружены файлы `server.js` и `package.json`
- [ ] Ожидаю ответа поддержки

---

## ✅ После установки Node.js

Когда поддержка включит Node.js:

1. Проверьте версию:
   ```bash
   node --version
   ```

2. Продолжите с шага 8 из [TIMEWEB_STEP_BY_STEP.md](TIMEWEB_STEP_BY_STEP.md):
   - Установка зависимостей: `npm install`
   - Создание .env файла
   - Запуск через PM2

---

## 🆘 Если поддержка откажет

Если Timeweb не может предоставить Node.js:

1. **Используйте Railway.app** - бесплатно, 5 минут
   - См. [RAILWAY_STEP_BY_STEP.md](RAILWAY_STEP_BY_STEP.md)

2. **Используйте PHP-прокси** (если PHP доступен)
   - Могу создать PHP версию

3. **Используйте VPS** с предустановленным Node.js

---

**Сообщите мне:** 
1. Создали ли тикет в поддержку?
2. Загрузили ли файлы в `smart-column-proxy`?
3. Что показывает `php --version` (на случай альтернативы)?

