# Документация cloud_proxy

## ⚠️ ПРАВИЛО

**Все файлы документации (.md) должны создаваться в директории `docs/` или `data/docs/`, а не в корне проекта.**

Исключение: `README.md` может находиться в корне директории для быстрого доступа, но он должен быть кратким и ссылаться на полную документацию в `docs/`.

## 📁 Структура

Эта директория содержит документацию для модуля cloud_proxy.

Основная документация по развертыванию находится в `data/docs/`.
