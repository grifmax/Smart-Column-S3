# Исправление проблемы: login.php возвращает 404

## 🔍 Проблема

При заходе на `https://spiritcontrol.ru/login.php` возвращается ошибка 404 - файл не найден.

## ✅ Решение

### 1. Проверьте структуру файлов на сервере

Файл `login.php` должен находиться в корне директории, где работает сайт.

**Если сайт работает через основной домен `smartcolumn/public_html/`:**
- Загрузите `login.php` в `smartcolumn/public_html/login.php`

**Если сайт работает через поддомен `smart-column-proxy/public_html/`:**
- Загрузите `login.php` в `smart-column-proxy/public_html/login.php`

### 2. Проверьте, какие файлы уже загружены

Проверьте наличие следующих файлов:
- `proxy.php` - должен быть в корне
- `login.php` - должен быть в корне (этого файла не хватает!)
- `auth_web.php` - должен быть в корне
- `web_settings_api.php` - должен быть в корне
- `esp32_config.php` - должен быть в корне
- `.htaccess` - должен быть в корне
- `web/` - директория с веб-интерфейсом

### 3. Загрузите недостающие файлы

Из локальной директории `cloud_proxy/` загрузите на сервер:

**Обязательные файлы:**
1. `login.php` - страница авторизации
2. `auth_web.php` - система авторизации
3. `web_settings_api.php` - API для настроек ESP32
4. `esp32_config.php` - функции конфигурации ESP32
5. `config.php` - конфигурация (или создайте пустой файл)
6. `.htaccess` - правила Apache (обновленный)

### 4. Проверьте права доступа

После загрузки установите права:

```bash
chmod 644 login.php
chmod 644 auth_web.php
chmod 644 web_settings_api.php
chmod 644 esp32_config.php
chmod 644 config.php
chmod 644 .htaccess
```

### 5. Проверьте работу

1. Откройте `https://spiritcontrol.ru/login.php`
2. Должна открыться форма авторизации
3. Попробуйте войти с `admin:admin` (или зарегистрируйтесь)

### 6. Если проблема сохраняется

Проверьте:
- Правильность пути к файлу на сервере
- Настройки Apache (разрешена ли обработка PHP файлов)
- Логи ошибок сервера
- Правильность `.htaccess` (нет ли конфликтующих правил)

## 📋 Полный список файлов для загрузки

См. `data/docs/FILES_FOR_PROXY.md` для полного списка файлов.

