# Первые шаги: Развертывание на Timeweb

## ✅ Что уже настроено

Из вашей панели видно:
- ✅ Домен: `co111685.tw1.ru`
- ✅ HTTPS включен
- ✅ HTTP/2 включен
- ✅ SSH-консоль доступна

---

## ШАГ 1: Проверка Node.js

### Вариант A: Через панель управления

1. В левом меню найдите **"Инструменты"**
2. Раскройте его
3. Найдите пункт **"Node.js"** (если есть)
4. Нажмите на него

**Если Node.js есть:**
- Включите его
- Выберите версию (18.x или 20.x)

**Если Node.js нет:**
- Переходите к варианту B

### Вариант B: Через SSH-консоль

1. В панели: **"Инструменты"** → **"SSH-консоль"**
2. Нажмите на **"SSH-консоль"**
3. Откроется терминал в браузере

Выполните команды:

```bash
# Проверка Node.js
node --version

# Если Node.js установлен, увидите версию (например: v18.17.0)
# Если нет - увидите ошибку "command not found"
```

**Сообщите мне:** Что показывает `node --version`?

---

## ШАГ 2: Если Node.js не установлен

**✅ Подтверждено:** Node.js не установлен на вашем хостинге.

### Решение: Обращение в поддержку Timeweb

**📖 Подробная инструкция:** См. [TIMEWEB_NODEJS_SETUP.md](TIMEWEB_NODEJS_SETUP.md)

**Кратко:**
1. Создайте тикет в поддержку Timeweb
2. Попросите включить Node.js для аккаунта co111685
3. Обычно это делается в течение 1-24 часов

**Пока ждете ответа:**
- Подготовьте файлы (см. шаги ниже)
- Проверьте альтернативы (PHP, если доступен)

---

## ШАГ 3: Использование SSH-консоли Timeweb

### Преимущества SSH-консоли в панели:

- ✅ Не нужно устанавливать SSH клиент
- ✅ Работает прямо в браузере
- ✅ Уже настроена и готова к использованию

### Как использовать:

1. **"Инструменты"** → **"SSH-консоль"**
2. Откроется терминал
3. Выполняйте команды как в обычном терминале

---

## ШАГ 4: Проверка текущей директории

В SSH-консоли выполните:

```bash
# Показать текущую директорию
pwd

# Список файлов
ls -la

# Перейти в домашнюю директорию
cd ~
pwd
```

**Ожидаемый результат:** Что-то вроде `/home/c/co111685` или `/home/co111685`

**Сообщите мне:** Что показывает `pwd`?

---

## ШАГ 5: Создание директории для прокси

В SSH-консоли выполните:

```bash
# Перейти в домашнюю директорию
cd ~

# Создать папку для прокси
mkdir -p smart-column-proxy

# Перейти в неё
cd smart-column-proxy

# Проверить
pwd
ls -la
```

**Сообщите мне:** Создана ли директория? Что показывает `ls -la`?

---

## ШАГ 6: Загрузка файлов

### Вариант A: Через Git (если установлен)

В SSH-консоли:

```bash
cd ~/smart-column-proxy

# Проверить Git
git --version

# Если Git установлен:
git clone https://github.com/grifmax/Smart-Column-S3.git temp
mv temp/cloud_proxy/* .
mv temp/cloud_proxy/.* . 2>/dev/null || true
rm -rf temp

# Проверить файлы
ls -la
```

**Должны увидеть:** `server.js`, `package.json`

### Вариант B: Через файловый менеджер

1. В панели: **"Файл. менеджер"**
2. Перейдите в папку `smart-column-proxy`
3. Загрузите файлы:
   - `server.js`
   - `package.json`

**Сообщите мне:** Загружены ли файлы? Видите ли `server.js` и `package.json`?

---

## Что делать дальше

После выполнения этих шагов сообщите мне:

1. ✅ Установлен ли Node.js? Какая версия?
2. ✅ Создана ли директория `smart-column-proxy`?
3. ✅ Загружены ли файлы `server.js` и `package.json`?

И я дам следующие указания!

---

## Полезные команды для проверки

```bash
# Версия Node.js
node --version

# Версия npm
npm --version

# Текущая директория
pwd

# Список файлов
ls -la

# Проверка Git
git --version
```

