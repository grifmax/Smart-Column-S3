# Пошаговые указания: Развертывание на Timeweb

## 🎯 Что мы будем делать

Развернем облачный прокси-сервер на вашем хостинге Timeweb, чтобы подключаться к ESP32 из любой точки мира.

---

## ШАГ 1: Проверка Node.js в панели Timeweb

### Что делать:

1. Войдите в панель управления Timeweb (где вы сейчас)
2. В левом меню найдите **"Инструменты"**
3. Раскройте его и найдите **"Node.js"**
4. Нажмите на **"Node.js"**

### Что проверить:

- ✅ Node.js должен быть **включен**
- ✅ Должна быть доступна версия (18.x или 20.x)

### Если Node.js не включен:

- Нажмите кнопку **"Включить"** или **"Активировать"**
- Если такой кнопки нет - обратитесь в поддержку Timeweb

**Сообщите мне:** Включен ли Node.js? Какая версия доступна?

---

## ШАГ 2: Получение данных для SSH

### Что делать:

1. В панели Timeweb найдите **"Аккаунт"** в левом меню
2. Раскройте его и нажмите **"Настройки доступа"**
3. Найдите раздел **"SSH доступ"**

### Что записать:

Запишите или скопируйте:
- **Хост:** (обычно `vh456.timeweb.ru` или IP адрес)
- **Порт:** (обычно `22`)
- **Логин:** (ваш FTP логин, судя по скриншоту: `co111685`)
- **Пароль:** (тот же, что для панели управления)

**Сообщите мне:** Есть ли у вас SSH доступ? Какие данные для подключения?

---

## ШАГ 3: Создание поддомена

### Что делать:

1. В панели Timeweb найдите **"Домены"** в левом меню
2. Раскройте и нажмите **"Мои домены"**
3. Найдите ваш основной домен
4. Нажмите на него или найдите кнопку **"Поддомены"**

### Создание поддомена:

1. Нажмите **"Добавить поддомен"** или **"Создать поддомен"**
2. Заполните:
   - **Имя поддомена:** `smartcolumn` (или любое другое)
   - **Домен:** выберите ваш основной домен
   - **Директория:** `/smart-column-proxy` (или оставьте по умолчанию)
3. Нажмите **"Создать"** или **"Сохранить"**

**Результат:** Получите URL вида `smartcolumn.yourdomain.com`

**Сообщите мне:** Какой поддомен вы создали? Какой полный URL?

---

## ШАГ 4: Установка SSL сертификата

### Что делать:

1. В панели Timeweb перейдите в **"Домены"** → **"SSL-сертификаты"**
2. Найдите ваш поддомен из шага 3
3. Нажмите **"Установить"** или **"Let's Encrypt"** (бесплатный)
4. Дождитесь установки (обычно 1-2 минуты)

**Результат:** HTTPS будет работать автоматически

**Сообщите мне:** Установлен ли SSL сертификат? Работает ли HTTPS?

---

## ШАГ 5: Подключение по SSH

### Windows:

1. Откройте **PowerShell** или **Git Bash**
2. Выполните команду:
   ```bash
   ssh co111685@vh456.timeweb.ru
   ```
   (Замените на ваши данные из шага 2)

3. Введите пароль (он не будет отображаться при вводе)
4. Нажмите Enter

**Результат:** Вы должны увидеть приглашение командной строки сервера

### Если не работает:

- Установите Git for Windows (включает Git Bash)
- Или используйте PuTTY (скачайте с putty.org)

**Сообщите мне:** Удалось ли подключиться по SSH?

---

## ШАГ 6: Создание директории на сервере

### Что делать (в SSH терминале):

```bash
# Перейдите в домашнюю директорию
cd ~

# Создайте папку для прокси
mkdir -p smart-column-proxy

# Перейдите в неё
cd smart-column-proxy

# Проверьте, что вы в правильной директории
pwd
```

**Результат:** Должно показать что-то вроде `/home/c/co111685/smart-column-proxy`

**Сообщите мне:** Создана ли директория? Что показывает `pwd`?

---

## ШАГ 7: Загрузка файлов

### Вариант A: Через Git (если установлен)

Выполните в SSH:

```bash
cd ~/smart-column-proxy

# Клонируйте репозиторий
git clone https://github.com/grifmax/Smart-Column-S3.git temp

# Скопируйте файлы прокси
mv temp/cloud_proxy/* .
mv temp/cloud_proxy/.* . 2>/dev/null || true

# Удалите временную папку
rm -rf temp

# Проверьте, что файлы загружены
ls -la
```

**Должны увидеть:** `server.js`, `package.json`

### Вариант B: Через файловый менеджер Timeweb

1. В панели Timeweb откройте **"Файл. менеджер"**
2. Перейдите в папку `smart-column-proxy`
3. Загрузите файлы из локальной папки `cloud_proxy`:
   - `server.js`
   - `package.json`

**Сообщите мне:** Загружены ли файлы? Видите ли вы `server.js` и `package.json`?

---

## ШАГ 8: Установка зависимостей

### Что делать (в SSH):

```bash
cd ~/smart-column-proxy

# Установите зависимости
npm install
```

**Ожидаемый результат:**
```
added 50 packages in 10s
```

**Если ошибка:**
- Проверьте, что Node.js включен (шаг 1)
- Проверьте версию: `node --version`

**Сообщите мне:** Установились ли зависимости? Есть ли ошибки?

---

## ШАГ 9: Генерация токенов

### Что делать (в SSH):

```bash
# Сгенерируйте первый токен (для ESP32)
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Скопируйте результат** - это будет `ESP32_TOKEN`

```bash
# Сгенерируйте второй токен (для клиентов)
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Скопируйте результат** - это будет `CLIENT_TOKEN`

**Сообщите мне:** Какие токены вы сгенерировали? (Сохраните их в безопасном месте!)

---

## ШАГ 10: Создание .env файла

### Что делать (в SSH):

```bash
cd ~/smart-column-proxy

# Создайте файл .env
nano .env
```

### Что вставить:

Вставьте следующее (замените токены на ваши из шага 9):

```env
PORT=3000
ESP32_TOKEN=ваш_первый_токен_из_шага_9
CLIENT_TOKEN=ваш_второй_токен_из_шага_9
```

### Как сохранить в nano:

1. Нажмите `Ctrl+O` (сохранить)
2. Нажмите `Enter` (подтвердить имя файла)
3. Нажмите `Ctrl+X` (выйти)

**Сообщите мне:** Создан ли файл .env? Проверьте командой: `cat .env`

---

## ШАГ 11: Установка PM2

### Что делать (в SSH):

```bash
# Установите PM2 глобально
npm install -g pm2
```

**Ожидаемый результат:** Установка завершится без ошибок

**Если ошибка прав доступа:**
```bash
# Попробуйте с sudo (если доступен)
sudo npm install -g pm2
```

**Сообщите мне:** Установился ли PM2? Проверьте: `pm2 --version`

---

## ШАГ 12: Запуск прокси-сервера

### Что делать (в SSH):

```bash
cd ~/smart-column-proxy

# Запустите сервер через PM2
pm2 start server.js --name smart-column-proxy

# Настройте автозапуск
pm2 startup
# Выполните команду, которую выведет PM2 (если попросит)

# Сохраните конфигурацию
pm2 save
```

**Ожидаемый результат:**
```
[PM2] Starting smart-column-proxy
[PM2] Process started
```

**Проверьте статус:**
```bash
pm2 status
```

**Должно показать:** `smart-column-proxy` со статусом `online`

**Сообщите мне:** Запустился ли сервер? Что показывает `pm2 status`?

---

## ШАГ 13: Настройка проксирования (если нужно)

### Что проверить:

1. В панели Timeweb найдите настройки вашего поддомена
2. Проверьте, что указана правильная директория: `/smart-column-proxy`
3. Если есть настройки **"Проксирование"** или **"Reverse Proxy"**:
   - Укажите: `http://localhost:3000`

**Обычно Timeweb настраивает это автоматически!**

**Сообщите мне:** Нужно ли что-то настраивать в панели?

---

## ШАГ 14: Проверка работы

### Что делать:

1. Откройте браузер
2. Перейдите по адресу:
   ```
   https://smartcolumn.yourdomain.com/health
   ```
   (Замените на ваш поддомен из шага 3)

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "timestamp": 1234567890,
  "devicesCount": 0
}
```

### Если не работает:

1. Проверьте логи: `pm2 logs smart-column-proxy`
2. Проверьте статус: `pm2 status`
3. Проверьте, что поддомен указывает на правильную директорию

**Сообщите мне:** Работает ли `/health` endpoint? Что возвращает?

---

## ШАГ 15: Настройка Android приложения

### Что делать:

1. Откройте Android приложение
2. Перейдите в **Настройки устройства**
3. Нажмите **"Добавить устройство"** или **"+"**
4. Заполните:
   - **Название:** `Моя колонна (Timeweb)`
   - **Хост:** `smartcolumn.yourdomain.com` (ваш поддомен)
   - **Порт:** `443` (или оставьте пустым)
   - **Включите:** "Облачный прокси"
   - **Device ID:** `esp32-001` (или любой уникальный)
   - **Токен прокси:** ваш `CLIENT_TOKEN` из шага 9
   - **HTTPS:** Автоматически включено
5. Нажмите **"Проверить подключение"**
6. Если успешно - нажмите **"Сохранить"**

**Сообщите мне:** Подключается ли приложение? Видите ли вы данные?

---

## ШАГ 16: Настройка ESP32

### Что нужно сделать в коде ESP32:

Добавьте настройки подключения к прокси (см. `cloud_proxy/ESP32_INTEGRATION.md`):

```cpp
CloudProxySettings proxy;
proxy.enabled = true;
proxy.url = "smartcolumn.yourdomain.com";  // Ваш поддомен
proxy.deviceId = "esp32-001";  // Должен совпадать с приложением!
proxy.token = "ваш_ESP32_TOKEN_из_шага_9";
```

**Сообщите мне:** Настроили ли ESP32? Подключается ли он к прокси?

---

## ✅ Готово!

После выполнения всех шагов у вас должно работать:
- ✅ Прокси-сервер запущен на Timeweb
- ✅ HTTPS настроен
- ✅ Android приложение подключается
- ✅ ESP32 подключается (после настройки)

---

## Полезные команды для проверки

```bash
# Статус сервера
pm2 status

# Логи в реальном времени
pm2 logs smart-column-proxy

# Перезапуск
pm2 restart smart-column-proxy

# Проверка порта
netstat -tuln | grep 3000
```

---

## Если что-то не работает

1. **Проверьте логи:** `pm2 logs smart-column-proxy`
2. **Проверьте статус:** `pm2 status`
3. **Проверьте .env:** `cat ~/smart-column-proxy/.env`
4. **Проверьте файлы:** `ls -la ~/smart-column-proxy`

**Сообщите мне:** Что именно не работает? Какие ошибки видите?

