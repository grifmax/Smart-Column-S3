# Исправление ошибки "type 'String' is not a subtype of type 'Map<String, dynamic>'"

## ❌ Ошибка

```
type 'String' is not a subtype of type 'Map<String, dynamic>'
```

## 🔍 Причина

Приложение ожидает JSON объект, но получает строку. Это происходит потому что:
- Приложение делает запрос на `/api/status`
- Мок-сервер не имел этого эндпоинта

## ✅ Решение

### Шаг 1: Обновите мок-сервер

Мок-сервер обновлен и теперь поддерживает:
- ✅ `/api/status` - статус системы (как ESP32)
- ✅ `/api/health` - проверка здоровья
- ✅ `/api/process/start` - запуск процесса
- ✅ `/api/process/stop` - остановка процесса

### Шаг 2: Перезапустите мок-сервер

Остановите текущий сервер (Ctrl+C) и запустите заново:

```bash
cd cloud_proxy
node mock_server.js
```

### Шаг 3: Перезапустите приложение

1. Закройте приложение полностью
2. Откройте заново
3. Попробуйте подключиться

---

## 📝 Проверка

После перезапуска мок-сервера проверьте:

```bash
# Проверка health
curl http://localhost:3000/api/health

# Проверка status
curl http://localhost:3000/api/status
```

Оба должны вернуть JSON объекты, а не строки.

---

## 🔧 Если ошибка сохраняется

1. **Убедитесь, что мок-сервер перезапущен**
2. **Проверьте настройки подключения:**
   - URL правильный
   - Порт 3000
   - HTTPS выключен
3. **Очистите кэш приложения:**
   - Настройки → Приложения → Smart Column S3 → Очистить кэш
4. **Перезапустите приложение**

---

**После перезапуска мок-сервера ошибка должна исчезнуть!** ✅


