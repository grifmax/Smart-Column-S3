# Настройка подключения к облачному серверу в Android приложении

## 📋 Что нужно для подключения

Для подключения к облачному прокси-серверу вам понадобятся:

1. **URL облачного сервера** (например: `your-app.railway.app` или `your-domain.com`)
2. **Device ID** - уникальный идентификатор вашего ESP32 (например: `esp32-001` или MAC адрес)
3. **Токен прокси** - токен для авторизации (CLIENT_TOKEN из Railway)

---

## 🚀 Пошаговая инструкция

### Шаг 1: Откройте приложение

Запустите приложение **Smart Column S3** на вашем телефоне.

### Шаг 2: Добавьте устройство

1. Если устройство не подключено, вы увидите экран **"Устройство не подключено"**
2. Нажмите кнопку **"Добавить устройство"** (или иконку `+` в правом нижнем углу)

### Шаг 3: Настройте облачный прокси

В форме настройки устройства:

1. **Включите "Облачный прокси"**
   - Найдите переключатель **"Облачный прокси"**
   - Включите его (переключатель должен стать активным)

2. **Введите URL сервера**
   - В поле **"IP адрес или доменное имя"** введите URL вашего облачного сервера
   - Примеры:
     - `your-app.railway.app` (Railway)
     - `your-domain.com` (собственный домен)
     - `192.168.1.100` (локальный сервер, если облачный прокси на вашей сети)
   - ⚠️ **Не добавляйте** `http://` или `https://` - только домен или IP

3. **Введите Device ID**
   - В поле **"Device ID"** введите уникальный идентификатор вашего ESP32
   - Это может быть:
     - MAC адрес ESP32 (например: `AABBCCDDEEFF`)
     - Произвольный ID (например: `esp32-001`, `my-column-1`)
   - ⚠️ **Важно:** Device ID должен совпадать с тем, который использует ESP32 при подключении к серверу

4. **Введите токен прокси**
   - В поле **"Токен прокси"** введите CLIENT_TOKEN из настроек Railway
   - Это токен, который вы добавили в переменную `CLIENT_TOKEN` в Railway
   - Токен будет скрыт (поле с паролем)

5. **Порт** (обычно автоматически)
   - При включении облачного прокси порт автоматически меняется на `443` (HTTPS)
   - Если нужно, можете изменить вручную

6. **Название** (опционально)
   - Можете ввести удобное название, например: "Моя колонна"

### Шаг 4: Проверьте подключение

1. Нажмите кнопку **"Проверить подключение"**
2. Дождитесь результата:
   - ✅ **"Подключение успешно"** - все настроено правильно
   - ❌ **"Ошибка подключения"** - проверьте параметры

### Шаг 5: Сохраните настройки

1. Нажмите кнопку **"Сохранить"**
2. Приложение вернется на главный экран
3. Если ESP32 подключен к серверу, вы увидите данные с устройства

---

## 📝 Пример заполнения формы

```
Название: Моя колонна
IP адрес: your-app.railway.app
Облачный прокси: ✅ Включено
Device ID: esp32-001
Токен прокси: a1B2c3D4e5F6g7H8i9J0k1L2m3N4o5P6
Порт: 443
HTTPS: ✅ (автоматически включено)
```

---

## ✅ Проверка работы

После сохранения:

1. **Главный экран** должен показать подключенное устройство
2. **Статус подключения** должен быть зеленым
3. **Данные с датчиков** должны обновляться (если ESP32 подключен)

---

## 🔧 Решение проблем

### Ошибка: "Ошибка подключения"

**Возможные причины:**

1. **Неверный URL сервера**
   - Проверьте, что URL правильный (без `http://` или `https://`)
   - Проверьте, что сервер работает (откройте в браузере)

2. **Неверный Device ID**
   - Убедитесь, что Device ID совпадает с тем, который использует ESP32
   - Проверьте настройки ESP32

3. **Неверный токен**
   - Проверьте, что токен правильный (CLIENT_TOKEN из Railway)
   - Убедитесь, что нет лишних пробелов

4. **ESP32 не подключен к серверу**
   - ESP32 должен быть подключен к облачному серверу
   - Проверьте логи ESP32
   - Проверьте настройки ESP32

5. **Сервер недоступен**
   - Проверьте, что сервер работает (Railway Deployments)
   - Проверьте логи сервера

### Ошибка: "Таймаут подключения"

- Проверьте интернет-соединение
- Проверьте, что сервер доступен из интернета
- Попробуйте позже (возможно, сервер перезагружается)

### Устройство не отображается

- Убедитесь, что вы нажали "Сохранить"
- Перезапустите приложение
- Проверьте, что устройство сохранено в настройках

---

## 📚 Дополнительная информация

### Где взять параметры?

1. **URL сервера:**
   - Railway: Settings → Networking → Domain
   - Или ваш собственный домен

2. **Device ID:**
   - MAC адрес ESP32 (можно посмотреть в логах ESP32)
   - Или произвольный ID, который вы настроили в ESP32

3. **Токен прокси:**
   - Railway: Settings → Variables → CLIENT_TOKEN
   - Это токен, который вы добавили при настройке сервера

### Настройка ESP32

ESP32 должен быть настроен для подключения к облачному серверу. См.:
- `cloud_proxy/ESP32_INTEGRATION.md` - настройка ESP32
- `cloud_proxy/RAILWAY_STEP_BY_STEP.md` - развертывание сервера

---

## 🎯 Быстрая настройка

Если у вас уже есть:
- ✅ Облачный сервер (Railway)
- ✅ ESP32 настроен и подключен к серверу
- ✅ Токены сгенерированы

То просто:
1. Откройте приложение
2. Добавьте устройство
3. Включите "Облачный прокси"
4. Введите URL, Device ID и токен
5. Сохраните

**Готово!** ✅



