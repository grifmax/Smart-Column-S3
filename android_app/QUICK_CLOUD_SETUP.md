# Быстрая настройка облачного прокси в приложении

## 📱 Настройка в приложении (5 минут)

### Шаг 1: Откройте приложение
Запустите **Smart Column S3** на телефоне.

### Шаг 2: Добавьте устройство
1. Нажмите **"Добавить устройство"** (кнопка `+`)
2. Или откройте настройки устройства

### Шаг 3: Заполните форму

**Обязательные поля:**

1. **Включите "Облачный прокси"** ✅
   - Найдите переключатель и включите его

2. **URL сервера:**
   ```
   your-app.railway.app
   ```
   - Без `http://` или `https://`
   - Только домен (например: `smart-column-proxy.railway.app`)

3. **Device ID:**
   ```
   esp32-001
   ```
   - Или MAC адрес ESP32
   - Должен совпадать с настройками ESP32

4. **Токен прокси:**
   ```
   ваш_CLIENT_TOKEN_из_Railway
   ```
   - Это CLIENT_TOKEN из Railway Variables
   - Будет скрыт (поле с паролем)

5. **Порт:** `443` (автоматически при облачном прокси)

6. **Название:** (опционально) `Моя колонна`

### Шаг 4: Проверьте и сохраните
1. Нажмите **"Проверить подключение"**
2. Дождитесь **"Подключение успешно"** ✅
3. Нажмите **"Сохранить"**

---

## 📋 Где взять параметры?

### URL сервера
- **Railway:** Settings → Networking → Domain
- Скопируйте домен (например: `smart-column-proxy.railway.app`)

### Device ID
- MAC адрес ESP32 (можно посмотреть в логах)
- Или произвольный ID, который вы настроили в ESP32
- Примеры: `esp32-001`, `AABBCCDDEEFF`

### Токен прокси
- **Railway:** Settings → Variables → CLIENT_TOKEN
- Скопируйте значение токена

---

## ✅ Проверка

После сохранения:
- ✅ Устройство должно отображаться на главном экране
- ✅ Статус подключения зеленый
- ✅ Данные обновляются (если ESP32 подключен)

---

## 🔧 Проблемы?

### "Ошибка подключения"
- Проверьте URL (без http://)
- Проверьте Device ID (должен совпадать с ESP32)
- Проверьте токен (CLIENT_TOKEN из Railway)
- Убедитесь, что ESP32 подключен к серверу

### "Таймаут"
- Проверьте интернет
- Проверьте, что сервер работает (Railway Deployments)

---

**Подробная инструкция:** [CLOUD_PROXY_SETUP_APP.md](CLOUD_PROXY_SETUP_APP.md)



