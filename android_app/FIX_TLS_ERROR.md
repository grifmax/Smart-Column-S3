# Исправление ошибки TLS Handshake

## ❌ Ошибка

```
HandshakeException: Handshake error in client 
(OS Error: WRONG_VERSION_NUMBER)
```

## 🔍 Причина

Приложение пытается подключиться по **HTTPS** (TLS), но мок-сервер работает по **HTTP** (без TLS).

## ✅ Решение

### Вариант 1: Выключить HTTPS в приложении (рекомендуется)

1. Откройте настройки устройства в приложении
2. Найдите переключатель **"Использовать HTTPS"**
3. **Выключите его** ❌
4. Убедитесь, что порт = `3000` (не 443)
5. Сохраните настройки
6. Попробуйте подключиться снова

### Вариант 2: Проверьте настройки подключения

Убедитесь, что:
- ✅ **HTTPS:** Выключено
- ✅ **Порт:** `3000` (не 443)
- ✅ **URL:** IP адрес компьютера (не localhost)
- ✅ **Облачный прокси:** Включено
- ✅ **Device ID:** `test-device-001`
- ✅ **Токен:** `test_client_token_12345`

### Вариант 3: Пересоздайте подключение

1. Удалите текущее устройство
2. Добавьте устройство заново
3. **Важно:** При включении "Облачный прокси" **НЕ включайте HTTPS автоматически**
4. Вручную убедитесь, что HTTPS выключен

---

## 📝 Правильные настройки для локального тестирования

```
Название: Тестовое устройство
URL: 192.168.1.100 (IP вашего компьютера)
Порт: 3000
Облачный прокси: ✅ Включено
Device ID: test-device-001
Токен прокси: test_client_token_12345
HTTPS: ❌ ВЫКЛЮЧЕНО (важно!)
```

---

## 🔧 Если ошибка сохраняется

1. **Перезапустите мок-сервер:**
   ```bash
   cd cloud_proxy
   node mock_server.js
   ```

2. **Проверьте, что сервер работает:**
   ```bash
   curl http://localhost:3000/health
   ```

3. **Проверьте настройки в приложении:**
   - URL правильный (IP, не localhost)
   - Порт 3000
   - HTTPS выключен

4. **Перезапустите приложение**

---

## 💡 Почему это происходит?

- При включении "Облачный прокси" раньше автоматически включался HTTPS
- Но для локального мок-сервера нужен HTTP
- Теперь HTTPS можно включать/выключать независимо

---

**После исправления приложение должно подключиться успешно!** ✅


