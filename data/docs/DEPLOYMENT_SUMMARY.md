# Краткая сводка по развертыванию

## 📁 Структура на сервере

- **Основной домен:** `smartcolumn/public_html/`
- **Прокси сервер:** `smart-column-proxy/public_html/`

## 📦 Что загружать

### В `smart-column-proxy/public_html/`:

**PHP файлы (7 файлов):**
1. `proxy.php`
2. `login.php`
3. `auth_web.php`
4. `web_settings_api.php`
5. `esp32_config.php`
6. `config.php` (опционально)
7. `.htaccess`

**Директория:**
- `web/` (вся директория со всеми файлами, включая модифицированные `index.html` и `app.js`)

### В `smartcolumn/public_html/`:

**Один файл (выберите один вариант):**
- `.htaccess` (используйте `main_site_redirect.htaccess`)
- ИЛИ `index.php` (используйте `main_site_redirect.php`)

## ⚠️ Важно перед загрузкой

Перед копированием в `web/` нужно модифицировать:
1. `data/index.html` - добавить код из `ESP32_SETTINGS_HTML.txt`
2. `data/app.js` - добавить код из `web_esp32_settings.js`

Затем скопировать все из `data/` в `cloud_proxy/web/`

## 📚 Полная документация

См. файл `DEPLOYMENT_FILES_LIST.md` для полного списка и чек-листа.
Все инструкции в директории `docs/`.

