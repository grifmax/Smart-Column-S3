# Файлы для загрузки в smart-column-proxy/public_html/

## 📦 Все файлы для поддомена прокси

Загрузите все эти файлы в директорию `smart-column-proxy/public_html/`

### PHP файлы (в корне)

1. **proxy.php** - основной прокси файл
2. **login.php** - страница входа/регистрации
3. **auth_web.php** - система авторизации
4. **web_settings_api.php** - API для настроек ESP32
5. **esp32_config.php** - функции конфигурации ESP32
6. **config.php** - конфигурация (если нужен, или создайте пустой)
7. **.htaccess** - правила Apache

### Директория web/

Вся директория `web/` со всеми файлами веб-интерфейса ESP32:
- `web/index.html` (модифицированный с настройками ESP32)
- `web/app.js` (модифицированный с функциями настроек ESP32)
- `web/style.css`
- `web/charts.html`
- `web/calibration.html`
- `web/wifi.html`
- `web/manual.html`
- `web/charts.js`
- `web/charts-style.css`
- `web/column-animation.css`
- `web/column-animation.js`
- `web/schemes-animation.css`
- `web/version.json`
- `web/schemes/` (вся директория)

### Файлы конфигурации (создаются автоматически)

Эти файлы создадутся автоматически при первом использовании, но можно создать пустыми:

- `esp32_config.json` - настройки подключения к ESP32
- `users_web.json` - пользователи системы

## 📋 Порядок загрузки

1. Загрузите все PHP файлы в корень `smart-column-proxy/public_html/`
2. Загрузите файл `.htaccess` в корень
3. Загрузите всю директорию `web/` (со всеми поддиректориями)

## 🔒 Права доступа

После загрузки установите права:

```bash
cd smart-column-proxy/public_html/
chmod 644 *.php
chmod 644 .htaccess
chmod 755 web/
chmod 644 web/*
chmod 755 web/schemes/
chmod 644 web/schemes/*
chmod 666 esp32_config.json  # если создаете вручную
chmod 666 users_web.json     # если создаете вручную
```

## ✅ Проверка

После загрузки:

1. Откройте `https://smart-column-proxy.spiritcontrol.ru/login.php`
2. Должна открыться страница входа
3. Войдите с `admin:admin`
4. Должен открыться веб-интерфейс ESP32

