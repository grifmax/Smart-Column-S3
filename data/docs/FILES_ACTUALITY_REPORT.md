# Отчет об актуальности файлов в cloud_proxy/

## ✅ АКТУАЛЬНЫЕ (обязательно нужны)

### Основные PHP файлы:
1. **proxy.php** ✅ - основной прокси файл
   - Используется: да, основной файл
   - Зависимости: config.php, esp32_config.php, auth_web.php, web_settings_api.php

2. **login.php** ✅ - страница авторизации
   - Используется: да, страница входа
   - Зависимости: auth_web.php

3. **auth_web.php** ✅ - система авторизации
   - Используется: да, используется proxy.php, login.php, web_settings_api.php

4. **web_settings_api.php** ✅ - API для настроек ESP32
   - Используется: да, вызывается из proxy.php
   - Зависимости: auth_web.php, esp32_config.php

5. **esp32_config.php** ✅ - функции конфигурации ESP32
   - Используется: да, используется proxy.php, web_settings_api.php

6. **config.php** ✅ - базовая конфигурация
   - Используется: да, используется proxy.php

### Конфигурация:
7. **.htaccess** ✅ - правила Apache
   - Используется: да, обязательно для маршрутизации

### Файлы для основного домена:
8. **main_site_redirect.htaccess** ✅ - редирект
9. **main_site_redirect.php** ✅ - альтернативный редирект

### Директория:
10. **web/** ✅ - веб-интерфейс ESP32
    - Используется: да, содержит все статические файлы

---

## ❌ НЕАКТУАЛЬНЫЕ (можно удалить)

### Старые backend файлы (не используются):
1. **backend.php** ❌ - старый backend API
   - Используется: нет
   - Комментарий: заменен на веб-прокси

2. **auth.php** ❌ - старый auth для backend API
   - Используется: нет
   - Комментарий: заменен на auth_web.php

3. **api.php** ❌ - старый API файл
   - Используется: нет

4. **api_proxy.php** ❌ - старый API proxy
   - Используется: нет

5. **index.php** ❌ - старый entry point для backend.php
   - Используется: нет
   - Комментарий: ссылается на backend.php, который не используется

### Старые серверы:
6. **server.php** ❌ - старый PHP сервер
   - Используется: нет

7. **server.js** ❌ - Node.js сервер
   - Используется: нет

8. **start_websocket.php** ❌ - WebSocket сервер
   - Используется: нет

9. **mock_server.js** ❌ - мок-сервер для тестирования
   - Используется: нет (только для разработки)

### Старые файлы:
10. **esp32_settings.php** ❌ - старая отдельная страница настроек
    - Используется: нет
    - Комментарий: заменена на вкладку в index.html

11. **copy_web_files.php** ❌ - вспомогательный скрипт
    - Используется: нет (вспомогательный, можно удалить после использования)

---

## 🔧 ОПЦИОНАЛЬНЫЕ (можно оставить для разработки)

### Docker (если не используется):
- **docker-compose.yml** ⚠️
- **Dockerfile** ⚠️

### Зависимости (если не используются):
- **composer.json** ⚠️ - PHP зависимости
- **package.json** ⚠️ - Node.js зависимости  
- **package-lock.json** ⚠️ - Node.js зависимости

---

## 📊 Статистика

- **Актуальных файлов:** 10 (включая .htaccess и директорию web/)
- **Неактуальных файлов:** 11 (можно удалить)
- **Опциональных файлов:** 5 (можно оставить или удалить)

---

## 🗑️ Рекомендуется удалить

```bash
# Старые backend файлы
backend.php
auth.php
api.php
api_proxy.php
index.php

# Старые серверы
server.php
server.js
start_websocket.php
mock_server.js

# Старые файлы
esp32_settings.php
copy_web_files.php
```

**Итого: 11 файлов можно безопасно удалить**

