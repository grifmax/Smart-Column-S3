# Список файлов для развертывания

## 📁 Структура на сервере

- **Основной домен:** `smartcolumn/public_html/`
- **Прокси сервер:** `smart-column-proxy/public_html/`

---

## 📦 Файлы для `smart-column-proxy/public_html/`

### PHP файлы (в корне директории)

1. ✅ `proxy.php`
2. ✅ `login.php`
3. ✅ `auth_web.php`
4. ✅ `web_settings_api.php`
5. ✅ `esp32_config.php`
6. ✅ `config.php` (если нужен, или создайте минимальный)
7. ✅ `.htaccess`

### Директория `web/` (вся директория со всеми файлами)

```
web/
├── index.html          (модифицированный с настройками ESP32)
├── app.js              (модифицированный с функциями ESP32)
├── style.css
├── charts.html
├── charts.js
├── charts-style.css
├── calibration.html
├── wifi.html
├── manual.html
├── column-animation.css
├── column-animation.js
├── schemes-animation.css
├── version.json
└── schemes/
    ├── column-animated.svg
    ├── distillation-animated.svg
    ├── hold-animated.svg
    └── mash-animated.svg
```

**Важно:** Файлы `index.html` и `app.js` должны быть модифицированы (см. `docs/ESP32_SETTINGS_INSTRUCTIONS.md`)

### Файлы конфигурации (создаются автоматически)

- `esp32_config.json` - создастся автоматически
- `users_web.json` - создастся автоматически

---

## 📦 Файлы для `smartcolumn/public_html/`

### Вариант 1: Редирект (рекомендуется)

Создайте файл `.htaccess`:
```apache
Redirect permanent / https://smart-column-proxy.spiritcontrol.ru/
```

Или файл `index.php`:
```php
<?php
header('Location: https://smart-column-proxy.spiritcontrol.ru/');
exit;
```

### Вариант 2: Информационная страница

См. `docs/FILES_FOR_MAIN.md` для примера `index.html`

---

## ✅ Чек-лист перед загрузкой

### Подготовка файлов:

- [ ] Модифицирован `data/index.html` (добавлены настройки ESP32)
- [ ] Модифицирован `data/app.js` (добавлены функции ESP32)
- [ ] Скопированы все файлы из `data/` в `cloud_proxy/web/`

### Загрузка на прокси сервер:

- [ ] Загружен `proxy.php`
- [ ] Загружен `login.php`
- [ ] Загружен `auth_web.php`
- [ ] Загружен `web_settings_api.php`
- [ ] Загружен `esp32_config.php`
- [ ] Загружен `.htaccess`
- [ ] Загружена директория `web/` (все файлы)

### Загрузка на основной домен:

- [ ] Создан `.htaccess` или `index.php` с редиректом

### После загрузки:

- [ ] Установлены права доступа (chmod 644 для PHP, 755 для директорий)
- [ ] Проверен доступ к `https://smart-column-proxy.spiritcontrol.ru/login.php`
- [ ] Проверен вход с `admin:admin`
- [ ] Проверена вкладка "Настройки" - есть карточка "Подключение к ESP32"

---

## 📚 Документация

Все инструкции находятся в директории `docs/`:
- `docs/DEPLOYMENT_QUICK_START.md` - быстрый старт
- `docs/FILES_FOR_PROXY.md` - подробный список для прокси
- `docs/FILES_FOR_MAIN.md` - файлы для основного домена
- `docs/ESP32_SETTINGS_INSTRUCTIONS.md` - как модифицировать файлы

