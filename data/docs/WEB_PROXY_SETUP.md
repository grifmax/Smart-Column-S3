# Smart-Column S3 - Web Proxy Setup

## 🎯 Описание

Веб-прокси для Smart-Column S3, который позволяет получить доступ к веб-интерфейсу ESP32 через spiritcontrol.ru. Все страницы работают точно так же, как при прямом подключении к ESP32 по локальной сети.

## ✨ Возможности

- ✅ Полный веб-интерфейс ESP32 через интернет
- ✅ Проксирование всех API запросов к ESP32
- ✅ Настройка подключения к ESP32 через веб-интерфейс
- ✅ Поддержка HTTP Basic Authentication
- ✅ Отдача статических файлов (HTML, CSS, JS)
- ✅ Кэширование статических ресурсов

## 📋 Требования

- PHP 7.4+ с расширением curl
- Apache с mod_rewrite (или Nginx)
- ESP32 должен быть доступен по сети (локально или через VPN/туннель)

## 🚀 Установка

### Шаг 1: Скопируйте веб-файлы

Создайте директорию `web` и скопируйте все файлы из `data/`:

```bash
mkdir -p cloud_proxy/web
cp -r data/* cloud_proxy/web/
```

Или на Windows:
```powershell
mkdir cloud_proxy\web
xcopy /E /I data cloud_proxy\web
```

### Шаг 2: Загрузите файлы на сервер

Загрузите на spiritcontrol.ru:
- `proxy.php` - основной прокси файл
- `esp32_settings.php` - страница настроек
- `.htaccess` - правила Apache
- `web/` - директория с веб-интерфейсом
- `config.php` - конфигурация (если нужно)

### Шаг 3: Установите права доступа

```bash
chmod 644 proxy.php esp32_settings.php .htaccess
chmod 755 cloud_proxy/web/
chmod 666 esp32_config.json  # Создастся автоматически
```

### Шаг 4: Настройте подключение к ESP32

1. Откройте `https://spiritcontrol.ru/esp32_settings.php`
2. Заполните настройки:
   - **Включить проксирование**: ✅
   - **IP адрес ESP32**: например, `192.168.1.100`
   - **Порт**: обычно `80`
   - **HTTPS**: если ESP32 использует SSL
   - **Логин/Пароль**: если требуется авторизация на ESP32
3. Нажмите "Проверить подключение"
4. Нажмите "Сохранить"

### Шаг 5: Готово!

Откройте `https://spiritcontrol.ru/` - вы увидите веб-интерфейс ESP32!

## 🔧 Настройка подключения к ESP32

### Локальная сеть

Если ESP32 в вашей локальной сети:
- IP адрес: локальный IP ESP32 (например, `192.168.1.100`)
- Порт: `80` (по умолчанию)

### Через интернет (требует настройки)

Для доступа к ESP32 через интернет нужно:

1. **VPN** (рекомендуется):
   - Настройте VPN сервер (WireGuard, OpenVPN)
   - Подключите сервер spiritcontrol.ru к VPN
   - Используйте VPN IP адрес ESP32

2. **Туннель (ngrok, cloudflare tunnel)**:
   - Создайте туннель к ESP32
   - Используйте публичный URL туннеля

3. **Проброс портов** (не рекомендуется из-за безопасности):
   - Настройте проброс портов на роутере
   - Используйте внешний IP адрес

## 📱 Использование

### Доступ к веб-интерфейсу

Просто откройте в браузере:
```
https://spiritcontrol.ru/
```

Все функции работают так же, как при прямом подключении к ESP32:
- Мониторинг в реальном времени
- Управление процессом
- Настройки и калибровка
- Графики и история
- И т.д.

### Настройки ESP32

Страница настроек:
```
https://spiritcontrol.ru/esp32_settings.php
```

## ⚠️ Важные замечания

### WebSocket

WebSocket соединения **не проксируются** через PHP напрямую. Есть несколько решений:

1. **Nginx reverse proxy** (рекомендуется):
   ```nginx
   location /ws {
       proxy_pass http://ESP32_IP:80/ws;
       proxy_http_version 1.1;
       proxy_set_header Upgrade $http_upgrade;
       proxy_set_header Connection "upgrade";
   }
   ```

2. **Apache mod_proxy_wstunnel**:
   ```apache
   RewriteEngine On
   RewriteCond %{HTTP:Upgrade} =websocket [NC]
   RewriteRule /ws(.*) ws://ESP32_IP:80/ws$1 [P,L]
   ```

3. **Использование только HTTP API** (без WebSocket):
   - Веб-интерфейс будет работать
   - Данные будут обновляться через polling (раз в 2 секунды)

### Безопасность

1. **Используйте HTTPS** на spiritcontrol.ru
2. **Не используйте проброс портов** без защиты
3. **Используйте VPN** для безопасного доступа к ESP32
4. **Настройте авторизацию** на ESP32 (HTTP Basic Auth)
5. **Ограничьте доступ** к странице настроек (можно добавить пароль)

## 🔍 Устранение проблем

### Ошибка 503 "ESP32 not configured"

**Проблема:** Не настроено подключение к ESP32

**Решение:** 
1. Откройте `esp32_settings.php`
2. Включите проксирование
3. Укажите IP адрес ESP32
4. Сохраните настройки

### Ошибка 502 "ESP32 connection error"

**Проблема:** Не удается подключиться к ESP32

**Решения:**
1. Проверьте, что ESP32 включен и доступен
2. Проверьте IP адрес и порт
3. Проверьте, что сервер spiritcontrol.ru может подключиться к ESP32 (firewall, сеть)
4. Проверьте логи ESP32
5. Попробуйте подключиться к ESP32 напрямую: `curl http://ESP32_IP/health`

### WebSocket не работает

**Проблема:** WebSocket соединения не устанавливаются

**Решение:** 
- Настройте проксирование WebSocket через Nginx/Apache (см. выше)
- Или используйте только HTTP API (данные будут обновляться через polling)

### Статические файлы не загружаются

**Проблема:** CSS, JS, изображения не загружаются (404)

**Решение:**
1. Проверьте, что директория `web/` существует и содержит файлы
2. Проверьте права доступа: `chmod 755 web/`
3. Проверьте путь в `.htaccess`

### Данные не обновляются

**Проблема:** Интерфейс загружается, но данные не обновляются

**Решения:**
1. Проверьте настройки ESP32 (подключение работает?)
2. Откройте консоль браузера (F12) - посмотрите ошибки
3. Проверьте, что API запросы доходят до ESP32 (логи ESP32)

## 📂 Структура файлов

```
cloud_proxy/
├── proxy.php              # Основной прокси файл
├── esp32_settings.php     # Страница настроек ESP32
├── .htaccess              # Правила Apache
├── config.php             # Конфигурация
├── esp32_config.json      # Настройки ESP32 (создается автоматически)
└── web/                   # Веб-интерфейс ESP32
    ├── index.html
    ├── style.css
    ├── app.js
    ├── charts.html
    ├── calibration.html
    ├── wifi.html
    └── ...
```

## 🔄 Обновление веб-интерфейса

При обновлении веб-интерфейса ESP32:

1. Скопируйте новые файлы из `data/` в `cloud_proxy/web/`
2. Очистите кэш браузера (Ctrl+F5)
3. Готово!

---

**Готово!** Теперь у вас есть полный веб-интерфейс ESP32 через spiritcontrol.ru! 🎉

