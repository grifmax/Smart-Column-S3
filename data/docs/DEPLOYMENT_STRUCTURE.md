# Структура файлов на сервере

## 📁 Директории на сервере

### 1. `smartcolumn/public_html/` - Основной сайт

Это основной домен, откуда происходит доступ к веб-интерфейсу.

**Файлы:**
- `index.html` - главная страница (редирект или ссылка на прокси)
- Или может быть редирект на поддомен

### 2. `smart-column-proxy/public_html/` - Прокси сервер

Здесь находится весь функционал прокси и веб-интерфейса ESP32.

**Структура:**
```
smart-column-proxy/public_html/
├── proxy.php                    # Основной прокси файл
├── login.php                    # Страница авторизации
├── auth_web.php                 # Система авторизации
├── web_settings_api.php         # API для настроек ESP32
├── esp32_config.php             # Функции конфигурации ESP32
├── config.php                   # Конфигурация (если нужен)
├── .htaccess                    # Правила Apache
├── web/                         # Веб-интерфейс ESP32
│   ├── index.html
│   ├── app.js
│   ├── style.css
│   └── ... (все файлы из data/)
├── esp32_config.json            # Настройки ESP32 (создается автоматически)
└── users_web.json               # Пользователи (создается автоматически)
```

## 🔄 Варианты организации

### Вариант 1: Все на поддомене прокси (рекомендуется)

Основной домен `smartcolumn` редиректит на `smart-column-proxy`.

**В `smartcolumn/public_html/index.php`:**
```php
<?php
header('Location: https://smart-column-proxy.spiritcontrol.ru/');
exit;
```

### Вариант 2: Основной домен использует прокси

Если основной домен должен показывать интерфейс, то можно использовать поддомен как API endpoint.

## 📋 Файлы для загрузки

См. `FILES_FOR_PROXY.md` - файлы для `smart-column-proxy/public_html/`
См. `FILES_FOR_MAIN.md` - файлы для `smartcolumn/public_html/` (если нужны)

